services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      # 배포 시 반드시 긴 무작위 문자열로 바꾸세요 (로그인 보안 키)
      JWT_SECRET: "change-me-to-a-long-random-string"
    volumes:
      - ./data:/data # 학생 진도 DB 저장 (db.json)
    restart: unless-stopped
